<!DOCTYPE html>
<html>
<head>
    <script src="/javascripts/json2.js" type="text/javascript"></script>
    <script src="/javascripts/home.js" type="text/javascript"></script>
    <style>
    body { margin:0;}
    #content { width:80%; margin:0 auto;}
    #skill { height: 100%; width: 40%; float: left; }
    #main { height: 100%; margin :0 0 0 40% }
    </style>
    <title></title>
</head>
<body>
<div id="content">
<div id="skill">
    <table border="1">
        <%
            var i = 0;
            for (var skillName in skills) {
                var skill = skills[skillName];
                if (i % columnNumber == 0) {
        %>
                <tr>
        <%
                }
        %>
        <td><input type="checkbox" id="<%=skill.name%>" onclick="onSkillClick(this)"/><%=skill.name%></td>
        <%

                if (i % columnNumber == columnNumber - 1) {
        %>
                </tr>
        <%
                }
                i++;
            }
        %>
    </table>
</div>

    <div id="main">
    <form method="post" action="search" id="form">
    <input type="hidden" name="skills"/>
            <div>
                <ul id="select">

                </ul>
            <input type="submit" value="搜索"/>
            </div>
    </form>
    </div>
</div>
</body>
</html>